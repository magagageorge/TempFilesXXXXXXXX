import { ViewPortDimensions } from './view-port-dimensions';

describe('ViewPortDimensions', () => {
  it('should create an instance', () => {
    expect(new ViewPortDimensions()).toBeTruthy();
  });
});
