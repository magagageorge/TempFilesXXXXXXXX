import { WfLinkPreviewDirective } from './wf-link-preview.directive';

describe('WfLinkPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new WfLinkPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
