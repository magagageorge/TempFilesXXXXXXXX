import { WfLinkifyPipe } from './wf-linkify.pipe';

describe('WfLinkifyPipe', () => {
  it('create an instance', () => {
    const pipe = new WfLinkifyPipe();
    expect(pipe).toBeTruthy();
  });
});
