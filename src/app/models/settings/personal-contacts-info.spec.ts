import { PersonalContactsInfo } from './personal-contacts-info';

describe('PersonalContactsInfo', () => {
  it('should create an instance', () => {
    expect(new PersonalContactsInfo()).toBeTruthy();
  });
});
